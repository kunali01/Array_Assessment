using System;

namespace ArrayIntermediateProg
{
    internal class ReplaceNegative
    {
        //public static void Main(string[] args)
        //{
        //    int[] arr = { 1, 2, -3, 2, -4, -6 };

        //    for (int i = 1; i < arr.Length; i++)
        //    {
        //        if (arr[i] < 0)
        //        {
        //            arr[i] = arr[i - 1] * arr[i - 1];
        //        }
        //    }

        //    Console.WriteLine("Array with negatives replaced: " + string.Join(", ", arr));
        //}
    }
}
